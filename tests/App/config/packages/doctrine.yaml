doctrine:
    dbal:
        default_connection: def
        connections:
            def:
                driver: pdo_mysql
                url: "%def_database_url%"
                server_version: 5.7
            persistence:
                driver: pdo_mysql
                url: "%persistence_database_url%"
                server_version: 5.7
            monolithic:
                driver: pdo_mysql
                url: "%monolithic_database_url%"
                server_version: 5.7
